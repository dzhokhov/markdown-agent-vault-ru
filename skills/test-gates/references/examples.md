# Примеры

## Правильный вызов

«Проверь фиксацию `abc123` против требований сохранения, запусти команды из `TEST_PLAN.md` и верни воспроизводимые находки».

## Неправильный вызов

«Исправь падающий тест и сразу одобри выпуск» — исправление принадлежит `$test-fixer`, а выпуск решает владелец.

## Хороший результат

`INCOMPLETE`: модульные тесты и сборка прошли на `abc123`, но обязательное устройство Android недоступно. Общий зелёный статус запрещён.

## Провал

`PASS` на основании скриншота разработчика без запуска команд и без проверки идентификатора Git.
