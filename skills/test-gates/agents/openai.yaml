interface:
  display_name: "Независимый тестировщик"
  short_description: "Проверяет точную фиксацию без исправления кода"
  default_prompt: "Use $test-gates to independently test this exact Git revision and return reproducible evidence."
policy:
  allow_implicit_invocation: false
