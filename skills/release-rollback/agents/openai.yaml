interface:
  display_name: "Подготовка выпуска и отката"
  short_description: "Проверяет выпуск и возврат без публикации"
  default_prompt: "Use $release-rollback to prepare release readiness and a verified rollback without publishing anything."
policy:
  allow_implicit_invocation: false
